# 変更履歴

## V2.2.0 — 2026-08-18

### 追加

**動作ログをファイルに出力するようにしました（[#2](https://github.com/napspans/wireless-mic-battery-alert/issues/2)）**

EXE はコンソールを持たないため、これまで動作の記録がどこにも残りませんでした。監視が
どの状態にあるのか、再開に失敗していないか、どのデバイスを掴んだのかが外から分からず、
不具合の切り分けに時間がかかっていました。

実行ファイルと同じ場所の `logs/app.log` に記録します。タスクトレイの右クリックメニューに
「ログを開く」を追加しました。

記録するのは状態が変わったときだけです。

- 起動と終了（バージョン、設定ファイルの場所）
- 監視の開始・停止（手動 / 無操作連動 / デバイス変更の区別）
- 入力デバイスの解決結果とストリームの開閉
- アラート、自動一時停止、一時停止の解除
- 入力デバイスの取得失敗と、自動再開の失敗

### ログが増えすぎないようにした点

常駐して数秒ごとに巡回するアプリなので、素直に書くと際限なく増えます。3段構えで抑えて
います。

| 対策 | 内容 |
|---|---|
| サイズ上限 | 512KB × 3世代。**合計 1.5MB を超えません** |
| 重複の畳み込み | 直前と同じ内容は捨て、件数だけ次の行に添える |
| 既定は INFO | 巡回のたびには書かない。詳細は設定で明示的に有効化する |

重複の畳み込みは、再開の失敗のように巡回のたびに同じ失敗が続く状況を想定したものです。
500回続けて同じ記録をしても、残るのは1行と省略件数だけになります。

音声コールバックは毎秒40回以上呼ばれるため、そこからはログを出しません。

詳細ログが必要な場合は `config.json` の `debug_log` を `true` にしてください。巡回ごとの
記録が増えるため、常用は想定していません。

---

## V2.1.3 — 2026-08-18

### 修正

**スリープ復帰後に受信機を検出しなくなる問題**

入力デバイスを PortAudio のデバイス番号で設定に保存していましたが、この番号は固定では
ありません。スリープ復帰や USB の抜き差しでデバイスが再列挙されると番号が変わります。

実際に、設定に保存されていた 9 番は「スピーカー (Realtek(R) Audio)」（入力チャンネル 0）に
なっており、受信機は 12 番へ移っていました。アプリは存在しない入力を監視し続けるため、
送信機を入れても切っても何も検出しませんでした。

加えて、PortAudio のデバイス一覧は初期化時に作られたまま保持されます。常駐したままの
プロセスは再列挙後も古い一覧を見続けるため、番号のずれに気づけませんでした。

対処として次の2点を行いました。

- 入力デバイスを**名前**で保存し、起動時に現在の一覧から番号を引き直す。番号は補助情報
  として残し、現在の一覧に含まれる場合のみ使う
- ストリームを開く前にデバイス一覧を取り直す

既存の `config.json` はそのまま読めます。保存済みの番号が現在無効であれば、WASAPI の
既定デバイスへ自動的に退避します（設定画面を一度開いて保存すると、以後は名前で保持
されます）。

---

## V2.1.2 — 2026-08-18

### 修正

**スリープ復帰後にアラートが鳴り直す問題**

V2.1.1 で入れた「再開時に一時停止状態を引き継ぐ」修正が不完全でした。状態は引き継げて
いましたが、監視ループがそれを直後に打ち消していました。

`start()` は内部状態を「無音ではない」から始めます。デバイスが最初のブロックを返すまでの
間はこの初期値が読まれるため、ループからは「信号が戻った」ように見えます。ウォームアップ
中もこの判定が生きていたため、1秒後に一時停止が解除され、続いて本物のデジタル無音が届いて
アラートが鳴る、という流れになっていました。

無操作からの再開ではデバイスの再開が速く顕在化しにくい一方、スリープ復帰では再開に時間が
かかるため確実に発生していました。ウォームアップ中は「信号あり」を信用しないようにしました。

---

## V2.1.1 — 2026-08-18

### 修正

**自動停止から戻るたびにアラートが鳴る問題**

無操作からの再開時に一時停止状態とアラート回数をリセットしていたため、送信機を切った
まま席を外して戻ると、そのつどアラートと一時停止サウンドが鳴っていました。動画を見て
いて少し離れ、また操作するだけで鳴るため煩わしい状態でした。

自動停止からの再開では、一時停止していた事実を引き継ぐようにしました。手動での監視
開始は従来どおりリセットします。

### 変更

**通知音の出力デバイスを使わない間は手放すようにしました**

pygame のミキサーを起動時に初期化したまま抱えていましたが、鳴らす予定がない間まで
保持する必要はないため、再生時に初期化して20秒鳴らなければ解放するようにしました。

当初これを「スリープしない原因」と断定し、その後 V2.1.0 でも15分間スリープした記録が
あることから一転して「スリープとは無関係」と書きましたが、どちらも言い過ぎでした。

出力デバイスを掴んでいても電源要求が立つのは実際に音を鳴らした前後だけと見られます
（常時立つなら前述のスリープは起こり得ません）。V2.1.0 には後述のアラートが鳴り直す
問題があり、席に戻るたびに音が鳴っていたため、スリープできるかどうかが状況によって
変わる状態でした。この変更は音の乱発を止める修正と合わせて、その不安定さを減らします。

なお V2.1.0 で入れたマイクを閉じる修正は、`powercfg /requests` から USB Audio Device の
電源要求が消えることで裏付けが取れています。スリープを妨げる要因はこのアプリだけでは
なく、動画を再生しているブラウザなども電源要求を立てます。

---

## V2.1.0 — 2026-08-18

### 修正

**常駐中に Windows がスリープしなくなる問題を修正しました（[#1](https://github.com/napspans/wireless-mic-battery-alert/issues/1)）**

本アプリが監視のためにキャプチャストリームを開いたままにしていると、USB オーディオ
ドライバが Windows に SYSTEM 電源要求を立て続け、PC がスリープに入れなくなっていました。
アプリ自身は `SetThreadExecutionState` を呼んでおらず、`powercfg /requests` にはプロセス
ではなく `[DRIVER] USB Audio Device` として現れるため、原因が分かりにくい状態でした。

従来の自動一時停止はアラート音を止めるだけでストリームは開いたままだったため、この
問題は解消されませんでした。今回、**PC の無操作が続いたらストリームそのものを閉じる**
ようにしました。操作を再開すると自動で開き直します。

- 無操作の判定は `GetLastInputInfo` による。既定のしきい値は 180 秒
- 自動停止・自動再開では通知音を鳴らさない（手動での停止・再開と区別するため）
- 手動で監視を停止した場合は、操作を再開しても自動では復活しない
- スリープ復帰直後に入力デバイスが戻っていない場合は、次の巡回で開き直す
- 他のアプリ（Discord・OBS 等）がマイクを使用している間は監視を継続する。その間は
  そのアプリ由来で電源要求が立つため、こちらが閉じてもスリープしないため

### 追加

- 設定画面にバージョンと最終更新日を表示するようにしました
- タスクトレイの右クリックメニューに「設定ファイルの場所を開く」を追加しました
- 詳細設定タブに「スリープ連動」の設定を追加しました
  - 無操作で自動停止（既定: 有効）
  - 無操作と判定するまでの秒数（既定: 180、範囲 30〜1800）
  - 他アプリ使用中は継続（既定: 有効）

### 変更

- タスクトレイのアイコンを刷新しました。角丸スクエアにマイクのグリフを載せた
  デザインになり、状態を色で示します。無操作による自動停止を表す水色を追加しました
- アラート間隔の既定値を 30 秒から **10 秒** に変更しました
- 詳細設定タブがスクロールできるようになりました

### 既知の制約

- Discord などがマイクを掴んでいる間は設計どおり監視を継続します。この間はそのアプリ
  由来で電源要求が立つためスリープしません。詳細設定の「他アプリ使用中は継続」を
  オフにすると監視側は止まります
- ストアアプリ（Microsoft Store 経由のアプリ）のマイク使用は検出対象外です
- `powercfg /requests` に現れる「レガシー カーネルの呼び出し元」は別系統の要因であり、
  本修正では解消しません

---

## V2.0.0 — 2026-07-29

検出方式を音量しきい値から**信号途絶**の検知へ刷新しました。

Windows 11 の Voice Clarity（ノイズ抑制 APO）が有効な環境では、送信機が生きていても
静かな場所では -100 dB 以下まで落ち、電池切れと区別できなくなっていました。送信機の
電源が切れると受信機が完全なデジタル無音（厳密に 0 のサンプル）を出力する点に着目し、
ゼロサンプル率で判定するようにしました。音量レベルに依存しないため、しきい値の調整が
不要になりました。

あわせて、入力デバイスを WASAPI から明示的に選ぶよう是正し、しきい値方式を前提とした
設定項目（無音閾値・無音継続時間・検出方式・スヌーズ解除秒数・未実装だったスタート
アップ起動）を削除しました。通知音は単一ワーカーによる直列再生になり、状態が続けて
変わっても音が重ならなくなりました。

## V1.0.0 — 2026-04-02

初回リリース。音量しきい値によるワイヤレスマイクの電池切れ検知、通知音の設定、
タスクトレイ常駐、Windows 向け EXE 配布に対応。
